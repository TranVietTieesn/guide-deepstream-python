# Lesson 01 Extensions

- Change message frequency from every 30 frames to per-frame.
- Run with `--no-display` and observe pure broker mode.
- Extend vehicle/person fields.
- Comment out broker branch and keep only preview branch.
