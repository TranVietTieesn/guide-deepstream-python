# Lesson 01 Extensions

- Doi `message` theo frame number.
- Gan them mot field so vao custom struct.
- Thu attach meta o source co different timing va so sanh log.
- Thu in ra `frame_meta.frame_num` va `batch_meta.num_frames_in_batch`.
